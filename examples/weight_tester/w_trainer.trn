trainer weight_model;
  data
    va1, prs1, tmp1, alt1, curr_w1 using
      file("weight_training_best.csv", 
            vind_kias,prs,tmp,alt_msl,curr_w );
  model
    features: va1, prs1, tmp1, alt1;
    labels: curr_w1;
    algorithm:
      weight_poly(degree: 1, cutoff: 2.5);
end;
