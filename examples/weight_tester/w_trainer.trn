trainer w_model_trainer;
  data
    time, v_a, prs, tmp, alt_i, curr_w using file("weight_training_2.csv");
  model
    features: v_a, prs, tmp, alt_i;
    labels: curr_w;
    algorithm:
      weight;
end;
