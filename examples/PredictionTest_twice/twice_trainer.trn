trainer twice_trainer;
  constants
    x = 2;
    c = 0;
  data
    a, b using file("twice_training.csv");
  model
    features: a;
    labels: (x * a) + c;
    algorithm:
      lin_reg(fit_intercept: true);
end;
